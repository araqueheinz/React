// We are going to maintain the browser history ourselves
// History is a independent library installed with react router dom
import { createBrowserHistory } from 'history'; 

export default createBrowserHistory();